<%= form_for(match) do |f| %>
  <% if match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
      <% match.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% @match.h_score ||= Random.rand(0..10) %>
  <% @match.a_score ||= Random.rand(0..10) %>

  <div class="field">
    <%= f.label :away %>
    <%= f.text_area :away %>
  </div>

  <div class="field">
    <%= f.label :a_score %>
    <%= f.number_field :a_score %>
  </div>

  <div class="field">
    <%= f.label :home %>
    <%= f.text_area :home %>
  </div>

  <div class="field">
    <%= f.label :h_score %>
    <%= f.number_field :h_score %>
  </div>

  <div class="field">
    <%= f.label :week %>
    <%= f.number_field :week %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
